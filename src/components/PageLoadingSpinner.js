import React from 'react'
import classes from '../styles/PageLoader.module.css'

const PageLoadingSpinner = () => {
    return (
        <div className={classes.loader}></div>
    )
}

export default PageLoadingSpinner